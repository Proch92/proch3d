#version 130

in vec3 inPosition;
in vec3 inNormal;

out vec3 Normal;

uniform mat4 view;
uniform mat4 proj;
uniform mat4 model;

void main() {
	gl_Position = proj * view * model * vec4(inPosition, 1.0);

	Normal = inNormal;
}