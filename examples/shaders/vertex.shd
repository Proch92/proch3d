#version 130

in vec3 inPosition;
in vec3 inNormal;

out vec3 Normal;
out vec3 pos;

uniform mat4 view;
uniform mat4 proj;
uniform mat4 model;
uniform mat4 rot;

void main() {
	gl_Position = proj * view * model * vec4(inPosition, 1.0);

	Normal = inNormal;
	pos = inPosition;
}